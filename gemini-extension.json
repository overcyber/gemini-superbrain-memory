{
    "name": "gemini-superbrain-memory",
    "version": "1.0.0",
    "description": "Persistent memory for Gemini CLI using SuperBrain/OpenMemory",
    "settings": [
        {
            "name": "Memory Backend API Key",
            "description": "API key for the local SuperBrain/OpenMemory backend (kept in legacy env SUPERMEMORY_API_KEY for compatibility)",
            "envVar": "SUPERMEMORY_API_KEY",
            "sensitive": true
        }
    ],
    "contextFileName": "GEMINI.md",
    "mcpServers": {
        "superbrain-memory": {
            "command": "node",
            "args": [
                "${extensionPath}${/}dist${/}server.js"
            ],
            "cwd": "${extensionPath}"
        }
    }
}
